# SPAM-MAIL.-PREDICTION-USING-AI-ML
Spam mail prediction using AI &amp; ML is the process of detecting unwanted emails automatically. Machine Learning models like Naïve Bayes and SVM are trained on labeled data (spam and non-spam). They analyze features such as keywords, links, and sender details.  
